write a plan to update the branch labels so that:
- every "top level" PR has a display like "base <- branch"
- every stacked PR only shows its branch
- there's a copy button next to every branch name

save the plan to PLAN.md in this directory.
